# user-node-app

CRUD for user app by node

### List users
GET /users

### Detail for user
GET /users/:id

### Create user
POST /users

### Update user
PUT /users/:id

### Destroy user
DELETE /users/:id
